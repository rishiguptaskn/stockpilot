/**
 * Components — populated during Month 1-2.
 *
 * The plan is to add shadcn/ui-derived primitives here (Button, Card, DataTable,
 * ScoreBadge, SkeletonCard, ErrorCard, EmptyCard) so they can be imported
 * consistently from @stockpilot/ui/components across all apps.
 */

export {};
